package com.portal.backend.controller;

public class InitializeProgramFilesController {
    // if any initialization related APIs are needed, they can be defined here
    // if program for first time fetch the file from database
    // if program is already have file then fetc from file system and update the path in database.
}
